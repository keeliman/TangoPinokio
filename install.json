{
  "run": [{
    "method": "shell.run",
    "params": {
      "message": "git clone https://github.com/CyberAgentAILab/TANGO.git ."
    }
  }, {
    "method": "shell.run",
    "params": {
      "message": {
        "windows": "conda create -n tango_env python=3.9 -y && conda activate tango_env && pip install -r requirements.txt",
        "linux": "conda create -n tango_env python=3.9 -y && conda activate tango_env && pip install -r requirements.txt",
        "darwin": "conda create -n tango_env python=3.9 -y && conda activate tango_env && pip install -r requirements.txt"
      }
    }
  }, {
    "method": "notify",
    "params": {
      "html": "TANGO installation completed! Please configure your OpenAI API key in config.py before running."
    }
  }]
} 